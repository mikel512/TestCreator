@using Microsoft.AspNetCore.Identity
@using TestCreator.Areas.Identity.Data

@inject SignInManager<TestUser> SignInManager
@inject UserManager<TestUser> UserManager


@if (SignInManager.IsSignedIn(User))
{
    <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post" id="logoutForm" class="navbar-right">
        <ul class="nav navbar-nav navbar-right">
            <li>
                <a class="nav-link" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
            </li>

            <li>
                <button type="submit" class="btn btn-outline-light">Logout</button>
            </li>
        </ul>
    </form>
}
else
{
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" asp-area="Identity" asp-page="/Account/Register">Register</a>
        </li>
        @*<li><a asp-area="Identity" asp-page="/Account/Login" data-toggle="modal" data-target="#loginModal" onclick="loginModal()">Login</a></li>*@
        <a class="nav-link" asp-area="Identity" asp-page="/Account/Login">Login</a>
        @*<li class="nav-item">
            <a class="nav-link" data-toggle="modal" data-target="#loginModal">Login</a>
        </li>*@
    </ul>
}
